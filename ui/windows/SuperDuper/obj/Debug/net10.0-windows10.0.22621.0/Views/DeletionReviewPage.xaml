<!-- DeletionReviewPage: Review and execute deletion plan -->
<Page
    x:Class="SuperDuper.Views.DeletionReviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SuperDuper.ViewModels">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="Deletion Review" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="{Binding ViewModel.StatusMessage, Mode=OneWay}"
                       Style="{StaticResource SubtitleTextBlockStyle}" />
        </StackPanel>

        <!-- Summary cards -->
        <Grid Grid.Row="1" ColumnSpacing="16" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Files to Delete" Style="{StaticResource CaptionTextBlockStyle}" />
                    <TextBlock Text="{Binding ViewModel.FileCount, Mode=OneWay}"
                               Style="{StaticResource HeaderTextBlockStyle}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Space to Reclaim" Style="{StaticResource CaptionTextBlockStyle}" />
                    <TextBlock Text="{Binding ViewModel.FormattedTotalBytes, Mode=OneWay}"
                               Style="{StaticResource HeaderTextBlockStyle}" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Action buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
            <Button Content="Auto-Mark Duplicates"
                    Command="{Binding ViewModel.AutoMarkCommand, Mode=OneTime}" />
            <Button Content="Refresh"
                    Command="{Binding ViewModel.RefreshSummaryCommand, Mode=OneTime}" />
            <Button x:Name="ExecuteButton"
                    Content="Execute Deletion"
                    Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>

        <!-- Marked files list -->
        <Grid Grid.Row="3">
            <ProgressRing IsActive="{Binding ViewModel.IsLoadingFiles, Mode=OneWay}"
                          Visibility="{Binding ViewModel.IsLoadingFiles, Mode=OneWay}"
                          HorizontalAlignment="Left" Width="24" Height="24" Margin="0,4,0,0" />

            <TextBlock Text="No files are marked for deletion. Use Auto-Mark or mark files from the Duplicates view."
                       Visibility="{Binding ViewModel.HasNoFiles, Mode=OneWay}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Margin="0,32,0,0" />

            <ListView ItemsSource="{Binding ViewModel.MarkedFiles, Mode=OneTime}"
                      SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,2" Padding="12,8" CornerRadius="6"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Spacing="2">
                                    <TextBlock Text="{Binding FileName, Mode=OneTime}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBlock Text="{Binding DisplayPath, Mode=OneTime}"
                                               Style="{StaticResource PathTextStyle}" />
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="{Binding FormattedSize, Mode=OneTime}"
                                           VerticalAlignment="Center"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                                <Button Grid.Column="2"
                                        Content="&#xE838;"
                                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                                        Command="{Binding RevealInExplorerCommand, Mode=OneTime}"
                                        Background="Transparent" BorderThickness="0"
                                        Padding="6"
                                        VerticalAlignment="Center"
                                        ToolTipService.ToolTip="Reveal in Explorer" />

                                <Button Grid.Column="3" Content="Unmark"
                                        Command="{Binding UnmarkCommand, Mode=OneTime}"
                                        VerticalAlignment="Center"
                                        FontSize="12" Padding="10,4" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
