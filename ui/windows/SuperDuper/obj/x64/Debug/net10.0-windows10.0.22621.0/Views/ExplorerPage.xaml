<Page
    x:Class="SuperDuper.Views.ExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SuperDuper.Views"
    xmlns:controls="using:SuperDuper.Controls">

    <!-- 3-pane layout: Directory Tree | FileList | Comparison Pane -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*" MinWidth="180" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="45*" MinWidth="300" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="30*" MinWidth="220" />
        </Grid.ColumnDefinitions>

        <!-- Column 0: Directory tree -->
        <controls:DirectoryTreeControl Grid.Column="0"
                                       x:Name="DirectoryTree" />

        <!-- Column 1: Splitter -->
        <Border Grid.Column="1" Width="1"
                Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <!-- Column 2: File list -->
        <controls:FileListControl Grid.Column="2"
                                  x:Name="FileList" />

        <!-- Column 3: Splitter -->
        <Border Grid.Column="3" Width="1"
                Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <!-- Column 4: Comparison pane -->
        <controls:ComparisonPane Grid.Column="4"
                                 x:Name="ComparisonPaneControl" />
    </Grid>
</Page>
