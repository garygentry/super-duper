<Page
    x:Class="SuperDuper.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SuperDuper.Views"
    xmlns:vm="using:SuperDuper.ViewModels"
    xmlns:models="using:SuperDuper.Models">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="20" MaxWidth="1200">

            <!-- Page title + New Scan button -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Dashboard"
                           Style="{StaticResource TitleLargeTextBlockStyle}"
                           VerticalAlignment="Center" />
                <Button Grid.Column="1"
                        Content="New Scan"
                        Style="{StaticResource AccentButtonStyle}"
                        Command="{x:Bind ViewModel.OpenNewScanDialogCommand}"
                        AutomationProperties.Name="Open new scan dialog" />
            </Grid>

            <!-- Session picker -->
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Session:"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           VerticalAlignment="Center" />
                <ComboBox ItemsSource="{x:Bind ViewModel.SessionPickerItems}"
                          SelectedItem="{x:Bind ViewModel.SelectedSession, Mode=TwoWay}"
                          DisplayMemberPath="DisplayLabel"
                          MinWidth="380" />
            </StackPanel>

            <!-- 4 metric cards -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Files Scanned"
                                   Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Text="{x:Bind ViewModel.TotalFilesScanned, Mode=OneWay}"
                                   Style="{StaticResource TitleLargeTextBlockStyle}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Duplicate Groups"
                                   Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Text="{x:Bind ViewModel.TotalDuplicateGroups, Mode=OneWay}"
                                   Style="{StaticResource TitleLargeTextBlockStyle}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Wasted Space"
                                   Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock Text="{x:Bind ViewModel.FormattedWastedBytes, Mode=OneWay}"
                                   Style="{StaticResource TitleLargeTextBlockStyle}" />
                    </StackPanel>
                </Border>

                <!-- 4th card: Review Progress -->
                <Border Grid.Column="3" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Review Progress"
                                   Style="{StaticResource CaptionTextBlockStyle}" />
                        <ProgressRing IsActive="True"
                                      Value="{x:Bind ViewModel.ReviewProgressPercent, Mode=OneWay}"
                                      Maximum="100"
                                      Width="40" Height="40"
                                      AutomationProperties.Name="{x:Bind ViewModel.ReviewProgressAccessibleName, Mode=OneWay}" />
                        <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="{x:Bind ViewModel.ReviewedCount, Mode=OneWay}" />
                            <Run Text=" of " />
                            <Run Text="{x:Bind ViewModel.TotalReviewable, Mode=OneWay}" />
                            <Run Text=" reviewed" />
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Status message -->
            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <!-- Quick Wins Panel -->
            <StackPanel Spacing="8"
                        Visibility="{x:Bind ViewModel.HasQuickWins, Mode=OneWay}">
                <TextBlock Text="Quick Wins"
                           Style="{StaticResource SubtitleTextBlockStyle}" />
                <TextBlock Text="High-impact duplicates to review first."
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                <ItemsRepeater ItemsSource="{x:Bind ViewModel.QuickWins}">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="6" />
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="models:QuickWinItem">
                            <Border Style="{StaticResource CardBorderStyle}" Padding="12">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{x:Bind Category}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                        <TextBlock Text="{x:Bind Description}"
                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1"
                                               Text="{x:Bind FormattedSize}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
