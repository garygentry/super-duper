# Reusable Codex kickoff prompt

Use this in the existing task or a fresh task opened directly in the same local saved project:

```text
Continue the Super Duper Windows UI redesign in C:\Users\gary\workspace\super-duper.
Work directly in this existing checkout on codex/ui-redesign. Preserve the pre-redesign baseline
on wpf-poc at deefa40. Keep all redesign planning, implementation and testing on codex/ui-redesign;
do not create/switch branches, create another worktree, merge, rebase, push or delete branches.

Audit Git first and preserve unexpected edits. Read AGENTS.md, the current-control sections of
docs/windows-roadmap-session-handoff.md, plans/ui-redesign/README.md, execution-plan.md and
session-checkpoint.md. Follow codex-session-guide.md. Then read only the selected gate's linked
specifications, code and tests; do not load historical engine/campaign narratives without a need.

UIR-08 is complete. Read its ledger row, A01-A17 matrix and
evidence/uir-08-integration-regression.md. Rust Debug/Release each pass 226 tests with 10 ignored.
Windows Debug/Release each build cleanly and pass 220 Core, 76 Infrastructure and three loaded-STA
WPF methods, with five physical/provider/deletion skips. The A17 five-run real-worker restart/cache/
new/deleted/changed/exclusion/reread regression passes. Current automated visual regression has 191
Debug and 191 Release captures. Do not rerun this locally exhausted matrix without a code change or a
documented reopen condition.

The four-step current-screen native walkthrough also passes for the available environment. Its first
1180x760 and 900x600 observations exposed three fixture-only gaps: fixed file decisions, no folder
sets and a no-op Review folder Open set. The in-memory fixture now retains file/folder decisions and
revisions, supplies 25 two-copy folder sets and supports exact Review-to-Folders links. The operator's
corrected retest passed at both viewports. Narrator, Dark, the available named contrast theme, 150%
text and available 150%/175% monitor transitions passed, and changed settings were restored. NVDA and
physical 200% remain unrun/unavailable, not passed or waived.

The exact next action is UIR-09 final user workflow acceptance and durable completion assessment.
Review the UIR-08 evidence and scope, confirm no open critical redesign defect, request explicit
operator final acceptance, then update the durable completion package and commit it on this branch.
Do not repeat the completed native walkthrough or bundle another real-worker A17, physical long-scan,
provider or performance action.

Preserve UIR-07b exact-run contextual Performance from History/Progress/Summary, deterministic return
focus, partial/full cache and actual-read summaries, explicit unavailable values, qualified comparison,
25 history rows, six phase rows, 64 selectable device rows and the absence of raw sample/time-series
claims. Preserve UIR-07a highlighted/opened/active identity, immutable recorded settings, bounded
500-run History pages, 25-row/five-page current/terminal warnings, retained accepted pages, stable
result navigation and exact History/Progress/Summary return focus.

Preserve UIR-06a worker-owned combined totals, separate 200-row/five-page Files/Folders queries,
exact links, validation states and Check marked copies versus Check these copies. Preserve UIR-06b
ordered roots, virtual preview scope/revision/signature, confirmation/provenance, fresh-preview
enforcement, Reverse rule application, later manual overrides and Reset semantics. Retain all
UIR-04/05 monitoring, rescan, query, comparison, decision, navigation, reveal and focus contracts.

Preserve all production safety boundaries: DisabledRecycleOperationCapabilityExecutor,
CanSubmit=false, executorEnabled=false and no production execution action. Do not touch production
state, recovery outcomes, consumed campaigns or parked release-validation work. NVDA and physical
200% remain unavailable for A11; do not troubleshoot Windows, force scaling, install software
or substitute a claim. Do not start physical/provider/performance work without its distinct authority.

Runtime at the UIR-08 handoff: PID 67748 was absent; the initial fixture was closed; corrected fixture
PID 15072 was left open after the passing retest. Re-audit it before reuse and never overwrite a running
output. No worker or production app/state was touched. After explicit UIR-09 acceptance, update the gate
ledger, durable package, compact checkpoint, reusable kickoff prompt and shared handoff; review the
complete diff and run only proportionate checks; commit the coherent work on this branch. Report honest
skipped/unavailable states, the commit and the next smallest action. Do not mark the redesign complete
before explicit UIR-09 user acceptance. End with a copyable continuation prompt tailored to the
committed checkpoint and exact next action.
```
