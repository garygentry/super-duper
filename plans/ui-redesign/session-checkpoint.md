# Current redesign session checkpoint

Updated 2026-09-13. [Execution plan](execution-plan.md) owns gate status.

- Required branch: `codex/ui-redesign`. Preserve `wpf-poc` at `deefa40`; no switch/worktree/merge/push.
- Latest implementation: **UIR-06a, the commit containing this checkpoint**, based on `4480398`
  (UIR-05c). UIR-00/01/02/03 complete; **UIR-04, UIR-05 and UIR-06 in_progress**. UIR-04a/b/c,
  UIR-05a/b/c and UIR-06a are locally implemented.
- [UIR-06a evidence](evidence/uir-06a-review-overview.md): dedicated selected-scan Review overview
  with worker-owned combined totals; independent bounded/virtualized Files and Folders pages;
  exact-set return links; retained accepted pages on replacement errors; and separate current/stale
  revision plus ready/blocked/needs-review presentation. **Check marked copies** is explicitly
  whole-plan and distinct from the Files visible-page **Check these copies** action. The exact
  non-deleting build notice is visible; no execution action exists.
- Verification: full Core **214 passed**; **three loaded-STA WPF methods passed**; fixture build has
  zero warnings/errors. The fixture verifies 1180x760 and 900x600 Review reachability, independent
  virtualized lists, vertical-only outer scrolling, boundary/action access, toolbar stress,
  Light/Dark and 150% text enlargement. There are 105 accepted captures under
  `artifacts/uir06a/captures-accepted-reviewed`; accepted TRX files and retained corrections are
  under `artifacts/uir06a/results` and sibling isolated outputs.
- **Exact next local slice: UIR-06b focused Location preferences in Review.** Recompose the existing
  discoverable workflow into a focused panel while preserving saved ordered roots, virtual preview
  scope/revision/signature, explicit scope/count confirmation, provenance, application reversal and
  later manual overrides. Label preview changes separately from saved decisions; use **Reverse rule
  application**. Do not add arbitrary undo, change Reset semantics, enable execution, resolve
  recovery outcomes or pull UIR-07 forward.
- Retain UIR-04a rescan/restart/history evidence, UIR-04b freshness/terminal contracts, UIR-04c compact
  monitoring and all UIR-05 query/comparison/decision semantics. UIR-06b must retain Review's 200-row
  pages, independent five-page caches, worker revisions/combined totals and exact bounded links; no
  global flat plan.
- Retain UIR-03 scoped acceptance, Narrator, Dark/Desert/live text-size, 150%/175% transition/focus/
  selection and prior paired Debug/Release plus Rust 226 passed/10 ignored. No campaign was rerun.
- NVDA and physical 200% remain `unrun_unavailable` for UIR-08/A11; do not troubleshoot Windows,
  force scaling or install software. Full A17/integrated/native/operator acceptance remains later.
- Runtime: PID 67748/session 1 was re-audited responsive at the exact older UIR-03f fixture executable
  and left untouched. UIR-04b/04c/05a/05b/05c/06a fixtures are built, not running; UIR-06a used
  isolated outputs. Re-audit before reuse and never overwrite running outputs. No production
  app/worker or state was touched; test windows close.
- Boundaries: production deletion disabled; `DisabledRecycleOperationCapabilityExecutor`,
  `CanSubmit=false`, `executorEnabled=false`, absent execution action, engine/cache/protocol/query
  ceilings and survivor/revision/overlap protections unchanged. SOP10 complete; release validation
  parked; no campaign authority.
- Update evidence, plan/checkpoint/handoff, review/test and commit each coherent slice. At handoff,
  print a complete copyable continuation prompt tailored to the actual commit and next slice.
