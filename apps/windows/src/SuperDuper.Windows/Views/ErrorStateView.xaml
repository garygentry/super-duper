<UserControl x:Class="SuperDuper.Windows.Views.ErrorStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Margin="20"
            Padding="16"
            VerticalAlignment="Top"
            BorderBrush="#C84B31"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="Something needs attention" />
                <TextBlock Margin="0,5,16,0" Text="{Binding ContentErrorMessage}" TextWrapping="Wrap" />
            </StackPanel>
            <Button Grid.Column="1" Padding="10,5" Command="{Binding ClearContentErrorCommand}" Content="Dismiss" />
        </Grid>
    </Border>
</UserControl>
