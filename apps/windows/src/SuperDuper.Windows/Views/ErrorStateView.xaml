<UserControl x:Class="SuperDuper.Windows.Views.ErrorStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Margin="20"
            Padding="16"
            VerticalAlignment="Top"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitle}" Text="Something needs attention" />
                <TextBlock Margin="0,5,16,0" Text="{Binding ContentErrorMessage}" TextWrapping="Wrap" />
            </StackPanel>
            <Button Grid.Column="1" Command="{Binding ClearContentErrorCommand}" Content="Dismiss" />
        </Grid>
    </Border>
</UserControl>
