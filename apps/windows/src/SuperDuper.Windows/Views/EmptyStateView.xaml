<UserControl x:Class="SuperDuper.Windows.Views.EmptyStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="520">
        <TextBlock HorizontalAlignment="Center" Style="{StaticResource PageTitle}" Text="No sessions yet" />
        <TextBlock Margin="0,9,0,18"
                   HorizontalAlignment="Center"
                   Style="{StaticResource ShellBody}"
                   Text="Create a reusable session, choose one or more folders, and start finding duplicates."
                   TextAlignment="Center"
                   TextWrapping="Wrap" />
        <Button HorizontalAlignment="Center"
                Command="{Binding Sessions.NewSessionCommand}"
                Content="Create a session" />
    </StackPanel>
</UserControl>
