<UserControl x:Class="SuperDuper.Windows.Views.SessionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Margin="16,18,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Style="{StaticResource SectionTitle}"
                       Text="Sessions" />
            <Button Grid.Column="1"
                    Command="{Binding RefreshCommand}"
                    Content="Refresh"
                    ToolTip="Reload sessions" />
        </Grid>

        <Grid Grid.Row="1">
            <ListBox x:Name="SessionsList"
                     AutomationProperties.AutomationId="SessionsList"
                     AutomationProperties.Name="Scan sessions"
                     Margin="8,0"
                     HorizontalContentAlignment="Stretch"
                     ItemsSource="{Binding Items}"
                     SelectedItem="{Binding SelectedSession, Mode=TwoWay}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource DefaultListBoxItemStyle}">
                        <Setter Property="AutomationProperties.Name" Value="{Binding Name}" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="8,7">
                            <TextBlock FontWeight="SemiBold" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Margin="0,3,0,0" Style="{StaticResource CaptionText}" Text="{Binding StatusText}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Margin="20"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar Width="120" Height="4" IsIndeterminate="True" />
                <TextBlock Margin="0,10,0,0" Text="Loading sessions…" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="12,12,12,16">
            <TextBlock Margin="4,0,4,8"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       Text="{Binding ErrorMessage}"
                       TextWrapping="Wrap"
                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button
                    HorizontalContentAlignment="Left"
                    Command="{Binding NewSessionCommand}"
                    Content="＋  New session" />
        </StackPanel>
    </Grid>
</UserControl>
