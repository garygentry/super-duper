<UserControl x:Class="SuperDuper.Windows.Views.DuplicateFilesView"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="170" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock FontSize="22" FontWeight="SemiBold" Text="Duplicate files" />
                <TextBlock Margin="0,4,0,0"
                           Opacity="0.68"
                           Text="Results belong to the selected historical run and stay server-paged." />
            </StackPanel>
            <TextBox x:Name="FileSearch"
                     Grid.Column="1"
                     Margin="12,0,0,0"
                     VerticalContentAlignment="Center"
                     automation:AutomationProperties.Name="Path search"
                     automation:AutomationProperties.AutomationId="FileSearch"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Search duplicate file paths" />
            <TextBox x:Name="FileMinimumSize"
                     Grid.Column="2"
                     Width="145"
                     Margin="8,0,0,0"
                     VerticalContentAlignment="Center"
                     automation:AutomationProperties.Name="Minimum file size in bytes"
                     automation:AutomationProperties.AutomationId="FileMinimumSize"
                     Text="{Binding MinimumSizeText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Minimum file size in bytes" />
            <CheckBox x:Name="FileAcrossDrives"
                      Grid.Column="3"
                      Margin="10,0,2,0"
                      VerticalAlignment="Center"
                      automation:AutomationProperties.AutomationId="FileAcrossDrives"
                      automation:AutomationProperties.Name="Show only duplicate sets across multiple drives"
                      Content="Across drives"
                      IsChecked="{Binding AcrossDrives}" />
            <Button x:Name="FileApplyFilters"
                    Grid.Column="4"
                    automation:AutomationProperties.AutomationId="FileApplyFilters"
                    Margin="8,0,0,0"
                    Padding="13,7"
                    Command="{Binding ApplyFiltersCommand}"
                    Content="Apply filters" />
            <WrapPanel Grid.Row="1"
                       Grid.ColumnSpan="5"
                       Margin="0,10,0,0"
                       VerticalAlignment="Center"
                       automation:AutomationProperties.Name="Selected-root facet controls">
                <TextBlock Margin="0,0,8,0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Text="Selected root" />
                <ComboBox x:Name="FileSelectedRootFacet"
                          Width="360"
                          MaxDropDownHeight="280"
                          VerticalContentAlignment="Center"
                          automation:AutomationProperties.AutomationId="FileSelectedRootFacet"
                          automation:AutomationProperties.Name="Selected root facet; choose All selected roots to remove this filter"
                          DisplayMemberPath="DisplayText"
                          ItemsSource="{Binding SelectedRootFacetOptions}"
                          SelectedItem="{Binding SelectedRootFacet, Mode=TwoWay}">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="automation:AutomationProperties.Name" Value="{Binding DisplayText}" />
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
                <Button Margin="8,0,0,0"
                        Padding="10,4"
                        automation:AutomationProperties.AutomationId="FileRootFacetMostSets"
                        automation:AutomationProperties.Name="Sort selected roots by most matching sets"
                        Command="{Binding SortRootFacetsByCountCommand}"
                        Content="Most sets" />
                <Button Margin="6,0,0,0"
                        Padding="10,4"
                        automation:AutomationProperties.AutomationId="FileRootFacetNameSort"
                        automation:AutomationProperties.Name="Sort selected roots by name"
                        Command="{Binding SortRootFacetsByNameCommand}"
                        Content="Root A–Z" />
                <Button Margin="8,0,0,0"
                        Padding="10,4"
                        automation:AutomationProperties.AutomationId="FilePreviousRootFacets"
                        Command="{Binding PreviousRootFacetPageCommand}"
                        Content="Previous roots" />
                <Button Margin="6,0,0,0"
                        Padding="10,4"
                        automation:AutomationProperties.AutomationId="FileNextRootFacets"
                        Command="{Binding NextRootFacetPageCommand}"
                        Content="Next roots" />
                <TextBlock Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           Opacity="0.68"
                           Text="{Binding RootFacetCountText}" />
                <ProgressBar Width="70"
                             Height="4"
                             Margin="10,0,0,0"
                             IsIndeterminate="True"
                             Visibility="{Binding IsRootFacetLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           automation:AutomationProperties.AutomationId="FileRootFacetError"
                           automation:AutomationProperties.Name="Selected-root facet error"
                           Foreground="#C84B31"
                           Text="{Binding RootFacetErrorMessage}"
                           TextTrimming="CharacterEllipsis"
                           Visibility="{Binding HasRootFacetError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           automation:AutomationProperties.AutomationId="FileSelectedRootFilterText"
                           Opacity="0.68"
                           Text="{Binding SelectedRootFilterText}"
                           TextTrimming="CharacterEllipsis" />
            </WrapPanel>
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <UniformGrid automation:AutomationProperties.AutomationId="FileReviewSummary"
                         automation:AutomationProperties.Name="Duplicate file review summary"
                         Columns="4">
                <Border Margin="0,0,6,0" Padding="12,9" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Opacity="0.68" Text="Matching sets" />
                        <TextBlock automation:AutomationProperties.AutomationId="FileSummaryMatchingSets" FontSize="19" FontWeight="SemiBold" Text="{Binding MatchingSetCountText}" />
                    </StackPanel>
                </Border>
                <Border Margin="2,0,4,0" Padding="12,9" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Opacity="0.68" Text="Copies in those sets" />
                        <TextBlock automation:AutomationProperties.AutomationId="FileSummaryMatchingCopies" FontSize="19" FontWeight="SemiBold" Text="{Binding MatchingCopyCountText}" />
                    </StackPanel>
                </Border>
                <Border Margin="4,0,2,0" Padding="12,9" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Opacity="0.68" Text="Potential recoverable" />
                        <TextBlock automation:AutomationProperties.AutomationId="FileSummaryRecoverable" FontSize="19" FontWeight="SemiBold" Text="{Binding PotentialRecoverableText}" />
                    </StackPanel>
                </Border>
                <Border Margin="6,0,0,0" Padding="12,9" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Opacity="0.68" Text="Largest opportunity" />
                        <TextBlock automation:AutomationProperties.AutomationId="FileSummaryLargestOpportunity" FontSize="19" FontWeight="SemiBold" Text="{Binding LargestOpportunityText}" />
                    </StackPanel>
                </Border>
            </UniformGrid>
            <Border Margin="0,8,0,0"
                    Padding="12,8"
                    automation:AutomationProperties.AutomationId="FileLocationSummary"
                    automation:AutomationProperties.Name="Duplicate file location coverage"
                    BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Margin="0,0,8,0" FontWeight="SemiBold" Text="Location coverage" />
                    <TextBlock Grid.Column="1"
                               automation:AutomationProperties.AutomationId="FileLocationSummaryText"
                               Text="{Binding LocationCoverageText}"
                               TextWrapping="Wrap" />
                </Grid>
            </Border>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DataGrid x:Name="GroupsGrid"
                      automation:AutomationProperties.AutomationId="FileGroupsGrid"
                      automation:AutomationProperties.Name="Duplicate file groups"
                      AutoGenerateColumns="False"
                      CanUserSortColumns="True"
                      EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      IsReadOnly="True"
                      ItemsSource="{Binding Groups}"
                      SelectedItem="{Binding SelectedGroup, Mode=TwoWay}"
                      SelectionMode="Single"
                      Sorting="OnGroupsSorting"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="*"
                                        MinWidth="180"
                                        Header="Representative label"
                                        SortMemberPath="RepresentativeName"
                                        Binding="{Binding RepresentativeName}" />
                    <DataGridTextColumn Width="90"
                                        CanUserSort="False"
                                        Header="Type"
                                        Binding="{Binding RepresentativeType}" />
                    <DataGridTextColumn Width="125"
                                        Header="Group size"
                                        SortMemberPath="GroupSize"
                                        Binding="{Binding GroupSize}" />
                    <DataGridTextColumn Width="105"
                                        Header="Copies"
                                        SortMemberPath="CopyCount"
                                        Binding="{Binding CopyCount}" />
                    <DataGridTextColumn Width="205"
                                        CanUserSort="False"
                                        Header="Location span"
                                        Binding="{Binding LocationSpan}" />
                    <DataGridTextColumn Width="145"
                                        Header="Recoverable"
                                        SortDirection="Descending"
                                        SortMemberPath="RecoverableBytes"
                                        Binding="{Binding RecoverableBytes}" />
                </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Row="1" Margin="0,9,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Opacity="0.68" Text="{Binding GroupCountText}" />
                <StackPanel Grid.Column="1" automation:AutomationProperties.Name="Duplicate file group paging" Orientation="Horizontal">
                    <Button Padding="10,4" Command="{Binding PreviousPageCommand}" Content="Previous" />
                    <Button Margin="8,0,0,0" Padding="10,4" Command="{Binding NextPageCommand}" Content="Next" />
                    <Button Margin="8,0,0,0" Padding="10,4" Command="{Binding ClearFiltersCommand}" Content="Clear filters" />
                </StackPanel>
            </Grid>

            <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                    Opacity="0.94"
                    Visibility="{Binding IsLoadingOverlayVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Width="150" Height="4" IsIndeterminate="True" />
                    <TextBlock Margin="0,9,0,0" Opacity="0.7" Text="Loading duplicate groups…" />
                </StackPanel>
            </Border>
            <Border Padding="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderBrush="#C84B31"
                    BorderThickness="1"
                    CornerRadius="7"
                    Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock MaxWidth="620" Text="{Binding ErrorMessage}" TextWrapping="Wrap" />
            </Border>
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsUnavailable, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock HorizontalAlignment="Center" FontSize="20" FontWeight="SemiBold" Text="Results unavailable" />
                <TextBlock Margin="0,6,0,0" MaxWidth="560" Opacity="0.68" Text="{Binding StateMessage}" TextAlignment="Center" TextWrapping="Wrap" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock HorizontalAlignment="Center" FontSize="20" FontWeight="SemiBold" Text="No duplicate files" />
                <TextBlock Margin="0,6,0,0" Opacity="0.68" Text="{Binding StateMessage}" />
            </StackPanel>
        </Grid>

        <GridSplitter Grid.Row="3" Height="8" HorizontalAlignment="Stretch" ResizeDirection="Rows" />

        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock automation:AutomationProperties.AutomationId="FileSelectedSetName"
                               FontSize="17"
                               FontWeight="SemiBold"
                               Text="{Binding SelectedGroup.RepresentativeName, TargetNullValue=Select a duplicate set}" />
                    <TextBlock Margin="0,3,0,0"
                               automation:AutomationProperties.AutomationId="FileSelectedSetExplanation"
                               Opacity="0.68"
                               Text="Exact content was verified at scan time. The representative label does not identify an original."
                               TextWrapping="Wrap" />
                    <TextBlock Margin="0,3,0,0"
                               automation:AutomationProperties.AutomationId="FileSelectedSetLocations"
                               Opacity="0.68"
                               Text="{Binding SelectedGroup.LocationSpan}"
                               TextWrapping="Wrap" />
                </StackPanel>
                <TextBlock Grid.Column="1" Opacity="0.68" Text="{Binding MemberCountText}" />
            </Grid>
            <DataGrid x:Name="MembersGrid"
                      Grid.Row="1"
                      automation:AutomationProperties.AutomationId="FileMembersGrid"
                      automation:AutomationProperties.Name="Files in selected duplicate group"
                      AutoGenerateColumns="False"
                      EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      IsReadOnly="True"
                      ItemsSource="{Binding Members}"
                      SelectionMode="Single"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="190" Header="Selected root">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SelectedRoot}" TextTrimming="CharacterEllipsis" ToolTip="{Binding SelectedRoot}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="*" MinWidth="220" Header="Relative path">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock automation:AutomationProperties.Name="{Binding Path}" Text="{Binding RelativePath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Width="70" Header="Drive" Binding="{Binding Drive}" />
                    <DataGridTextColumn Width="125" Header="Modified" Binding="{Binding Modified}" />
                    <DataGridTextColumn Width="95" Header="Size" Binding="{Binding Size}" />
                    <DataGridTemplateColumn Width="190" Header="Actions">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Padding="7,2"
                                            Command="{Binding DataContext.CopyPathCommand, ElementName=Root}"
                                            CommandParameter="{Binding}"
                                            Content="Copy path" />
                                    <Button Margin="6,0,0,0"
                                            Padding="7,2"
                                            Command="{Binding DataContext.RevealInExplorerCommand, ElementName=Root}"
                                            CommandParameter="{Binding}"
                                            Content="Show in Explorer" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Row="2" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center"
                           automation:AutomationProperties.AutomationId="FileDetailError"
                           automation:AutomationProperties.Name="File action error"
                           Foreground="#C84B31"
                           Text="{Binding DetailErrorMessage}"
                           TextTrimming="CharacterEllipsis"
                           Visibility="{Binding HasDetailError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <StackPanel Grid.Column="1" automation:AutomationProperties.Name="Duplicate file member paging" Orientation="Horizontal">
                    <Button Padding="10,4" Command="{Binding PreviousMemberPageCommand}" Content="Previous copies" />
                    <Button Margin="8,0,0,0" Padding="10,4" Command="{Binding NextMemberPageCommand}" Content="Next copies" />
                </StackPanel>
            </Grid>
            <Border Grid.Row="1"
                    Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                    Opacity="0.94"
                    Visibility="{Binding IsDetailLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Width="130" Height="4" IsIndeterminate="True" />
                    <TextBlock Margin="0,8,0,0" Opacity="0.68" Text="Loading group members…" />
                </StackPanel>
            </Border>
            <StackPanel Grid.Row="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsDetailEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock HorizontalAlignment="Center" FontSize="17" FontWeight="SemiBold" Text="No copies to display" />
                <TextBlock Margin="0,5,0,0" Opacity="0.68" Text="This group no longer has visible members." />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
